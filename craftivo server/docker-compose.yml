version: '3.9'
services:
  db:
    image: postgres:16-alpine
    container_name: craftivo_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: samer
      POSTGRES_DB: craftivo_db
    ports:
      - '5433:5432'
    volumes:
      - craftivo_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  craftivo_pg_data:
